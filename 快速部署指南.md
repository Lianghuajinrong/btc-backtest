# 🚀 快速部署指南

## 第一步：推送代码到GitHub ✅

你的仓库URL：`https://github.com/Lianghuajinrong/btc-backtest.git`

**请在项目文件夹打开PowerShell，执行以下命令：**

```powershell
git init
git add .
git commit -m "Initial commit: BTC双均线回测系统"
git branch -M main
git remote add origin https://github.com/Lianghuajinrong/btc-backtest.git
git push -u origin main
```

**如果提示需要认证：**
- 访问 https://github.com/settings/tokens
- 点击 "Generate new token (classic)"
- 权限选择：`repo`（全部仓库权限）
- 复制生成的Token
- 推送时，用户名用你的GitHub用户名，密码用Token

---

## 第二步：部署后端到Railway

1. 访问 https://railway.app
2. 使用GitHub账号登录
3. 点击 **"New Project"**
4. 选择 **"Deploy from GitHub repo"**
5. 选择 `Lianghuajinrong/btc-backtest` 仓库
6. Railway会自动检测并部署（等待2-3分钟）
7. 部署完成后，点击项目 → **"Settings"** → **"Domains"**
8. **复制生成的URL**（例如：`https://btc-backtest-production.up.railway.app`）
9. **把URL发给我**，我会更新前端代码

---

## 第三步：我会帮你更新前端代码

收到Railway URL后，我会：
- 更新 `index.html` 中的API地址
- 提交并推送到GitHub

---

## 第四步：部署前端到Vercel

1. 访问 https://vercel.com
2. 使用GitHub账号登录
3. 点击 **"Add New Project"**
4. 选择 `Lianghuajinrong/btc-backtest` 仓库
5. Framework Preset 选择 **"Other"**
6. 点击 **"Deploy"**
7. 等待部署完成（约1分钟）
8. 获得前端URL（例如：`https://btc-backtest.vercel.app`）

---

## ✅ 完成！

现在你可以访问前端URL，使用在线版本的回测系统了！

---

## 💡 提示

- Railway和Vercel都有免费额度，足够个人使用
- 每次推送代码到GitHub，会自动重新部署
- 如果遇到问题，查看浏览器控制台（F12）的错误信息
